// ASSESSMENT 2: Coding practical questions with Jest

// Please read all questions thoroughly
// Pseudo coding is REQUIRED
// If you get stuck, please leave comments to help us understand your thought process

// Use test driven development to complete the following questions
// Add appropriate dependencies: $ yarn add jest

// Reminder: The test will call your function
// Run the file with the following command: $ yarn jest


// --------------------1) Create a function that takes a number as an argument and decides if the number is evenly divisble by three or not.
funtion DivideBy = {(number) % 3===0)
}
// using funtion DivideBy to equal the number in array divided by three
// return the number adding $ to add additional statement

  return(`${number} is divisible by three`)
}
// a) Create a test with expect statements for each of the variables provided.

var num1 = 15
funtion DivideBy = {(num1) % 3===0)
}
  return(`${num1} is divisible by three`)
}
// Expected output: "15 is divisible by three"
var num2 = 0

funtion DivideBy = {(num2) % 3===0)
}
  return(`${num2} is divisible by three`)
}
// Expected output: "0 is divisible by three"
var num3 = -7
funtion DivideBy = {(num3) % 3===0)}
{
  return(`${num3} is not divisibleb by three`)
}
// Expected output: "-7 is not divisible by three"


// b) Create the function that makes the test pass.



// --------------------2) Create a function that takes in an array of words and returns an array with all the words capitalized.
//

function capitalizedFirst = (randomNouns1) => {
  return.charAt(0).toUpperCase()
}
//use funtion capitalized and name it capitalizedFirst to equal array randomNouns1
//to equal return .charAt 0 representing first varible in string and add to toUpperCase the first
//letter of each string
// a) Create a test with expect statements for each of the variables provided.

var randomNouns1 = ["streetlamp", "potato", "teeth", "conclusion", "nephew"]
// Expected output: ["Streetlamp", "Potato", "Teeth", "Conclusion", "Nephew"]
let randomNouns1 = ["streetlamp", "potato", "teeth", "conclusion", "nephew"]
let result = ["Streetlamp", "Potato", "Teeth", "Conclusion", "Nephew"] => {
  return(capitalizedFirst(randomNouns1)).toUpperCase()
}

var randomNouns2 = ["temperature", "database", "chopsticks", "mango", "deduction"]
// Expected output: ["Temperature", "Database", "Chopsticks", "Mango", "Deduction"]
let randomNouns2 = ["temperature", "database", "chopsticks", "mango", "deduction"]
let result = ["Temperature", "Database", "Chopsticks", "Mango", "Deduction"] => {
  return(capitalizedFirst(randomNouns2)).toUpperCase()
}

// b) Create the function that makes the test pass.



// --------------------3) Create a function that takes in a string and logs the index of the first vowel.
let countVowels(string) {
let count = 0
  for (index i = 0; i string.length(); i++)
  return count;
}
// use let to use function count a varible and renaming it countVowels to count string
// make count variable 0 ,  index of length of string in array , make index at + 2
// to return count

// a) Create a test with expect statements for each of the variables provided.

var vowelTester1 = "learn"
// Expected output: 1
let count = 0
  for (index i = 0; i string.length("learn"); i++)
  return count;
}

var vowelTester2 = "academy"
// Expected output: 0
let count = 0
  for (index i = 0; i string.length("academy"); i++)
  return count;
}
var vowelTester3 = "challenges"
// Expected output: 2
let count = 0
  for (index i = 0; i string.length("challenges"); i++)
  return count;
}


// b) Create the function that makes the test pass.
